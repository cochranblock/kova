# Inventory of Source Files

## Rust Files (.rs)

- `android/src/lib.rs`
  - Kova Android entry point. `android_main` (NativeActivity) initializes Android logging, points `HOME` at internal storage so kova's paths resolve, calls `kova::bootstrap()`, then launches the eframe/egui GUI at 2.5 pixels-per-point for high-DPI phones with the `f320` theme. A `MobileApp` wrapper drives the soft keyboard (show/hide based on egui's `wants_keyboard_input`) and delegates rendering to the core `kova::gui::KovaApp`.
- `build.rs`
  - Workspace build script that only prints a loud `cargo:warning` documenting a known future-compat landmine: xml5ever 0.16.2 (pinned transitively via kalosm 0.4.0 → readability 0.2.0) uses a trailing semicolon in macro expression position, which will become a hard error in future Rust. The warning records the fix path: bump readability once kalosm 0.5+ ships.
- `edge/build.rs`
  - Build script for the `edge` crate (ESP32 firmware). Calls `embuild::espidf::sysenv::output()` so the build links against the ESP-IDF toolchain via embuild instead of the regular `esp-idf-sys` flow.
- `edge/src/main.rs`
  - ESP32-WROOM-32 firmware (Phase 1 of the edge plan). Sets up I2C on GPIO 21/22 at 100 kHz, initializes a BME280 sensor, then loops every 2 seconds printing temperature, humidity, and pressure as CSV over serial. Phase 2 (noted in the header) is on-device inference.
- `ios/src/lib.rs`
  - Kova iOS entry point. Exposes `kova_ios_main`, a C-ABI symbol called from Swift/ObjC in the Xcode project (built as a static lib for aarch64-apple-ios). Initializes oslog, sets `HOME` to the app-sandbox Documents dir, calls `kova::bootstrap()`, and runs the eframe/egui app at 2.0 pixels-per-point with the `f320` theme. `run_and_return` must stay false — with it true, eframe returns after the first frame and the caller re-invokes `kova_ios_main`, causing infinite recursion/stack overflow on M1.
- `patches/android-activity/build.rs`
  - Build script for the vendored `android-activity` crate patch. When the `game-activity` feature is on, uses the `cc` crate to compile the C++ sources vendored under `game-activity-csrc/` into three static libs: `libgame_activity.a` (GameActivity + GameActivityEvents), `libgame_text_input.a` (GameTextInput), and `libnative_app_glue.a` (Android's native app glue C file), all linked against `c++_static` plus an explicit `c++abi` link for the static C++ runtime.
- `patches/android-activity/src/config.rs`
  - Defines `ConfigurationRef`, a cheaply-cloneable, thread-safe handle (`Arc<RwLock<Configuration>>`) over the NDK's `Configuration` struct. Lets any thread read the latest device configuration (screen size/dp, density, keyboard, navigation, orientation, SDK version, night mode, etc.) without deep-copying the large struct; `replace()` updates it in place on configuration-change events so pre-existing references see the new values.
- `patches/android-activity/src/error.rs`
  - Error types for the crate. Public `AppError` (main-thread-only violations, Java/JNI errors, input unavailable) is what the API surface exposes; internal `InternalAppError` wraps `jni` crate errors (`JniError`, `JvmError`, Java exceptions) so the backends can use `From<jni::errors::Error>` without leaking the `jni-rs` dependency into the public API. An `InternalAppError -> AppError` conversion strips JNI details at the API boundary.
- `patches/android-activity/src/game_activity/ffi.rs`
  - FFI module root for the GameActivity backend. Bindings are pre-generated (via bindgen) rather than generated at build time; this file conditionally `include!()`s the correct per-architecture bindings — `ffi_arm.rs`, `ffi_aarch64.rs`, `ffi_i686.rs`, or `ffi_x86_64.rs` — selected by `target_arch` (or the `test` feature for host testing). Carries the usual bindgen lint allowances.
- `patches/android-activity/src/game_activity/ffi_aarch64.rs`
  - Pre-generated bindgen FFI bindings for the aarch64 Android target (~7.7k lines, 2350 symbols). Declares the C APIs the GameActivity backend links against: JNI (`JavaVM*`), Android NDK (`AInputEvent`/`AKeyEvent`/`AMotionEvent`, `AInputQueue`, `AHardwareBuffer`, `AConfiguration`, looper), GameActivity C API (`GameActivity`, `GameActivityCallbacks`, motion/key event structs, soft input and window flags), GameTextInput, and the `android_app`/native-app-glue structs, plus the full Bionic libc surface (signal, time, sched, poll) that bindgen pulled in, with layout tests for each struct.
- `patches/android-activity/src/game_activity/ffi_arm.rs`
  - Pre-generated bindgen FFI bindings (rust-bindgen 0.66.1) for the 32-bit ARM target (`__WORDSIZE: 32`, NDK 25.2). Same shape as `ffi_aarch64.rs` — Bionic/NDK constants, JNI, Android NDK input/looper/hardware-buffer APIs, GameActivity and GameTextInput C APIs, and the `android_app` native-glue structs — with 32-bit-specific layout (e.g. `__WORDSIZE`, pointer size) baked in. Selected by `ffi.rs` when `target_arch` is `arm`/`armv7`.
- `patches/android-activity/src/game_activity/ffi_i686.rs`
  - Pre-generated bindgen FFI bindings (rust-bindgen 0.66.1) for the 32-bit x86 target (`__WORDSIZE: 32`, NDK 25.2). Same content as `ffi_aarch64.rs` — Bionic/NDK constants, JNI, NDK input APIs, GameActivity/GameTextInput C APIs, native-app-glue structs — with the 32-bit x86 ABI/layout. Selected by `ffi.rs` for `target_arch = x86` (mainly emulator/debug builds).
- `patches/android-activity/src/game_activity/ffi_x86_64.rs`
  - Pre-generated bindgen FFI bindings (rust-bindgen 0.66.1) for the 64-bit x86_64 target (`__WORDSIZE: 64`, NDK 25.2). Same content as `ffi_aarch64.rs` (Bionic/NDK constants, JNI, NDK input APIs, GameActivity/GameTextInput C APIs, native-app-glue structs) with the 64-bit x86_64 ABI/layout. Selected by `ffi.rs` for `target_arch = x86_64` (mainly emulator/debug builds).
- `patches/android-activity/src/game_activity/input.rs`
  - Safe Rust wrappers over GameActivity's double-buffered C input structs. Defines the `InputEvent` enum (`MotionEvent` / `KeyEvent` / `TextEvent`) plus `MotionEvent` (action, pointers via a `PointersIter` of `PointerImpl`, meta/button state, edge flags, timing) and `KeyEvent` (key code, scan code, repeat count, flags), each thinly wrapping the raw `GameActivityMotionEvent` / `GameActivityKeyEvent` C structs by reference. Modeled on android-ndk-rs's `event.rs`, with `Source`/`Class` enums defined from the Java docs since some sources aren't exposed via the AInputQueue API.
- `patches/android-activity/src/game_activity/mod.rs`
  - The GameActivity backend implementation. `AndroidAppInner` holds the JVM, `android_app` glue pointer, `ConfigurationRef`, `NativeWindow`, key-character maps, and the input receiver; `poll_events()` drains the ALooper + input queue and converts C key/motion events (via lending iterators over the double-buffered `InputBuffer`) into the crate's `PollEvent`s. Also implements soft-input show/hide, text input state bridging (with a C callback `map_input_state_to_text_event_callback`), `StateSaver`/`StateLoader` for the saved-state byte buffer, window flags, asset/data paths, and the C entry points — `android_main`, `GameActivity_onCreate`, `Java_com_google_androidgamesdk_GameActivity_initializeNativeCode`, and `_rust_glue_entry` — that the GameActivity C++ glue calls back into.
- `patches/android-activity/src/input/sdk.rs`
  - JNI bindings + safe wrappers over Android's `KeyCharacterMap`. The `pub(crate)` `KeyCharacterMapBinding` (shared by `Arc`) caches the class and method IDs for `get`, `getDeadChar`, and `getKeyboardType` up front so per-event calls are `call_method_unchecked` fast paths. The public `KeyCharacterMap` maps a keycode + meta-state to `KeyMapChar` (`Unicode` / `CombiningAccent` / `None`, decoding the `0x80000000` dead-key bit) and queries the keyboard type as a `KeyboardType` enum (Numeric/Predictive/Alpha/Full/SpecialFunction plus a `__Unknown` catch-all for SDK extensibility). All JNI failures are funneled through `jni_utils::clear_and_map_exception_to_err` so no `jni-rs` types leak into the public API.
- `patches/android-activity/src/input.rs`
  - Shared input vocabulary used by both backends. Defines `Source` (touchscreen, keyboard, gamepad, mouse, dpad, joystick, trackball, touchpad, etc. backed by a `SourceFlags` bitflag), the `MetaState`/`ButtonState`/`EdgeFlags`/`KeyEventFlags` bitflag structs, and the `MotionAction`, `KeyAction`, `Button`, `Axis`, `ToolType`, and large `Keycode` enums covering the full Android keycode table — all SDK-derived enums use the `__Unknown(u32)` catch-all convention. Also holds the soft-input `TextSpan`/`TextInputState` and the `Pointer`/`PointersIter`/`InputIterator` lending handles that the backend event iterators expose (thin wrappers over each backend's raw event, converting values through the crate's own enums so unknown future SDK variants survive round-trips).
- `patches/android-activity/src/jni_utils.rs`
  - JNI VM/exception utilities. `CloneJavaVM` is a cheaply-cloneable wrapper over `jni::JavaVM` (clone re-wraps the raw VM pointer) marked `Send + Sync` so it can be shared across threads. `clear_and_map_exception_to_err` is the `.map_err()` companion for JNI calls made outside a native method: since there's no Java frame to unwind into, it grabs the pending `JThrowable`, pulls `getMessage()` inside a local frame, clears the exception, and returns `InternalAppError::JniException` (a TODO notes fetching the Java backtrace is unimplemented). `device_key_character_map` looks up `InputDevice.getDevice(id).getKeyCharacterMap()` and wraps the result in a global ref to build a `KeyCharacterMap`.
- `patches/android-activity/src/lib.rs`
  - Crate root. Long module docs frame it as the Rust equivalent of `android_native_app_glue.c`: standalone apps build as `cdylib` with a `#[no_mangle] android_main`, and before `android_main` runs the crate forwards stdio to logcat, registers the `JavaVM`/`Activity` with `ndk_context`, and attaches a Looper to the native thread. `compile_error!` guards enforce that exactly one of the `game-activity`/`native-activity` features is on (with a message explaining double-linked-version failures and pointing at `[patch]`), and `activity_impl` is `#[cfg_attr]`-aliased to the selected backend module. Public API: `MainEvent` (lifecycle, window, focus, config, low-memory, `SaveState`/`Resume` with saver/loader), `PollEvent` (Wake/Timeout/Main), `InputStatus`, `WindowManagerFlags`, `Rect`, and the main handle `AndroidApp` — an `Arc<RwLock>`-based, cheaply-clonable, `Send + Sync` object exposing `poll_events`, native window/config/content-rect access, window flags, motion-axis toggles, soft input + text input state, input-event iteration, per-device key character maps, SDK version, and data/asset paths.
- `patches/android-activity/src/native_activity/glue.rs`
  - The C-facing half of the NativeActivity backend. `WaitableNativeActivityState` is the `android_app` struct handed to C, with mutex/condvar-guarded state shared between the Java-side `ANativeActivity` callbacks and the native thread; `NativeActivityState` holds the msg pipe fds (command channel), config, saved-state, input queue, window, content rect, activity/thread state, and pending window/queue slots. The extern "C" `on_*` callbacks (lifecycle, window created/resized/redraw/destroyed, input queue created/destroyed, config/focus/low-memory/content-rect changes, save-instance-state) update state under the lock and signal the condvar. `pre_exec_cmd`/`post_exec_cmd` serialize/deserialize `AppCmd`s (with `TryFrom<i8>`) across the pipe into the native thread's event stream, and `request_save_state` implements the round-trip save-state handshake. `ANativeActivity_onCreate` is the C entry point that sets up state, forwards stdout/stderr to logcat, spawns the native thread, and invokes the app's `android_main`.
- `patches/android-activity/src/native_activity/input.rs`
  - NativeActivity input event wrappers. `MotionEvent` and `KeyEvent` are lifetime-scoped `#[repr(transparent)]`-style wrappers over `ndk::event::MotionEvent`/`KeyEvent` (with `into_ndk_event` to hand back to the input queue for disposal). They deliberately call `ndk_sys` C functions directly (e.g. `AInputEvent_getSource`, `AMotionEvent_getAction`) rather than ndk-rs methods, so values pass through the crate's own `Source`/`MotionAction`/`Keycode` enums and unknown variants added in newer Android SDKs are captured by the `__Unknown` catch-all instead of being dropped. `MotionEvent` exposes action/pointer metadata plus `pointers()`/`pointer_at_index()` returning the shared `Pointer`/`PointersIter` lending handles.
- `patches/android-activity/src/native_activity/mod.rs`
  - NativeActivity backend implementation (the `activity_impl` module when the `native-activity` feature is on). Constructs `AndroidAppInner` (JVM, `NativeActivityGlue`, ALooper pointer, `KeyCharacterMapBinding` + per-device `KeyCharacterMap` cache, input receiver) and prepares an `ALooper` attached to the cmd-pipe fd. `poll_events()` drains looper events — reading `AppCmd`s off the pipe, translating lifecycle/window/config/input-queue commands into `MainEvent`s, and attaching input queues to the looper — while `input_events_iter()` provides the lending iterator that drains the `AInputQueue`. Also implements `StateSaver`/`StateLoader` (backed by the save-state handshake), `AndroidAppWaker` (a cloneable `NonNull<ALooper>` handle whose `wake()` calls `ALooper_wake`, usable as an async task waker to interrupt a blocked poll), soft-input show/hide and text-input state bridging through the Java activity, and the public `android_main` entry point that spawns the app's `android_main` function.
- `patches/android-activity/src/util.rs`
  - Platform utilities. `try_get_path_from_ptr` converts a C string pointer to a `PathBuf` (None for null/empty/invalid UTF-8), `android_log` maps `log::Level` onto `__android_log_write` priorities, and `forward_stdio_to_logcat` dup2's stdout/stderr onto a `pipe2` pipe and spawns a "stdio-to-logcat" thread that line-reads it and forwards each line to logcat under the tag `RustStdoutStderr`. `log_panic` logs a caught panic payload (String/&str or "UnknownPanic"), and `abort_on_panic` runs a closure under `catch_unwind` and `abort()`s on panic — used at JNI callback boundaries because unwinding past JNI is UB (a TODO notes throwing a Java exception instead of aborting, since an Activity doesn't necessarily own the whole process).
- `src/academy.rs`
  - MoE-powered autonomous development agent ("the do-all module"). `explain_trace` (f115) keeps the old Kalosm-based signature for `serve.rs` compat but now asks the IRONHIVE cluster (`T193::default_hive`) to narrate a pipeline trace (intent, user msg, stage, outcome, retries, stderr, chain) with the project's Cursor rules injected as system context. `f301` is the main pipeline: `gather_context` builds a `T90` project snapshot (file count, line counts, per-file summaries, module list, commands, Cargo.toml), the cluster plans the task into `T182` steps (ReadFiles/GenerateFile/EditFile/RunCommand/GitCommit), each step executes via `moe_generate` (multi-expert) or `single_generate`, `verify_project` runs cargo check and `fix_step` runs a capped fix loop (DDI note: effectiveness drops after 2-3 attempts), and `do_git_commit` commits with a generated message when `auto_commit` is set. Configured by `T185` (project_dir, num_experts, max_fix_retries, num_ctx, auto_commit, dry_run) and returns `T186` (steps, success, files_changed).
- `src/agent_loop.rs`
  - Agentic tool loop (f147/f148, `t106`=AgentAction). `f147` runs one turn: streams LLM inference (f382 picks local/remote/auto), parses tool calls out of the accumulated response (tools::f140), executes each via tools::f141 with dimmed stderr status lines (ok/err + truncated preview), and returns `Done(response)` when no tool calls are found or `Continue { tool_results }` otherwise. `f148` drives turns up to `max_iterations`, auto-compacting the conversation via `context_mgr::f380` under a fixed 8k-token budget (1k system, 512 tools) and trimming each tool output to 1024 tokens (f172) before appending `[tool] ok/err: <preview>` lines to the conversation.
- `src/autopilot.rs`
  - Cursor autopilot (f121): types prompts into Cursor's agent composer with zero API cost. `focus_cursor` finds and activates the Cursor window via `xdotool search --name`, then `run` opens the composer (Cmd+I on macOS, Ctrl+I elsewhere) with `enigo`, sleeps for settle, types the prompt text, and hits Return.
- `src/backlog.rs`
  - Backlog types + loading (f25=load_backlog), formerly in kova-core. `t8` is one backlog.json entry (intent string plus optional project/approuter_dir/cmd/args) and `t9` wraps `items: Vec<t8>`. `f293` maps an entry to the intent enum (`t0`) for full-pipeline, tunnel-update, setup-roguerepo, cloudflare-purge, test, or custom (cmd + args), attaching the project path's file name as a hint; unknown intents return `None`. `f25` reads and serde-parses backlog.json. Includes unit tests for the intent mapping.
- `src/bin/bench-classify.rs`
  - bench-classify: measures the starter classifiers against held-out test sets, anchoring gap 12.2 (no standard-benchmark validation) in real numbers. Loads `starter.nanobyte`, runs the `intent_classifier` over `assets/datasets/kova_intents/test.csv` (33 examples, 10 classes), and prints total accuracy, macro-precision/recall/F1, the 10 lowest-F1 classes with support counts, and the 10 most frequent true→predicted confusion pairs. Adding another (model, dataset) pair is a one-line edit to the `BENCHES` table.
- `src/bin/carve_static.rs`
  - Static corpus carver. Walks `--input-dir` (default `/mnt/data/crates/`), in-memory extracts each `.crate` archive with a GzDecoder, syn-parses every `.rs` file via a `Visit` collector, and emits labeled JSONL per subatomic model to `--output-dir` (default `/mnt/data/training/static/`) — 15 models: async_detector, arg_count (0/1/2/3/4+), return_type, visibility, self_receiver, field_count, variant_count, error_enum, method_count, and the has_debug/clone/serialize/deserialize/default/partial_eq derive detectors. Supports `--max-crates` and progress logging; run behind the `carve` feature.
- `src/bin/kova-test.rs`
  - kova-test: thin binary wrapper (f90=kova_test) that delegates to `kova::f315()` and exits nonzero on error. Equivalent to `kova test`.
- `src/bin/pack-starter.rs`
  - pack-starter: consolidates trained subatomic models into `assets/starter.nanobyte`. Reads `assets/models/{name}/{config.json,weights.bin,bias.bin}` for the five starters (slop_detector, code_vs_english, lang_detector, intent_classifier, t2_coordinator), concatenates `[W | b]` into one f32 blob per model (`num_classes * feature_dim + num_classes` floats), validates that config.json's name matches the directory, and writes the packed file via `nanobyte::Nanobyte` + `PackInput` (manifest carries name/tier/feature_dim/num_classes/class_names so the reader can split the blob back).
- `src/bin/retrain-starters.rs`
  - retrain-starters: re-trains slop_detector / code_vs_english / lang_detector at FEATURE_DIM=8192 (gap analysis 12.1). The original starters used hash dim 256, whose Weinberger-2009 tail bound implies ~98% trigram collision rate — a hard quality cap; 8192 drops that to ~58% at the cost of ~290 KB of starter growth. Delegates to `swarm::train::f395` and writes into `assets/models/`.
- `src/bin/train-intent.rs`
  - train-intent: trains the banking77 intent classifier subatomic model. Reads `assets/datasets/banking77/{categories.json,train.csv}` (validated at 77 classes), trains a trigram-hash → linear classifier at FEATURE_DIM=4096 (~315K params, ~1.25 MB) over 30 epochs, and writes to `assets/models/intent_classifier/`. The 4096 hash dim is a documented compromise between the gap-analysis-flagged 256 (~98% collision floor) and 8192+ (~2.5 MB), landing at ~77% collisions per the Weinberger bound.
- `src/bin/train-kova-intent.rs`
  - train-kova-intent: trains the intent classifier on kova-domain prompts. Reads `assets/datasets/kova_intents/{categories.json,train.csv}`, trains a trigram-hash → linear classifier at FEATURE_DIM=4096 (50 epochs, LR 0.05) over 10 kova intent classes (fix_bug, add_feature, explain_code, refactor, write_tests, write_docs, review_code, run_command, search_code, project_info), and writes weights to `assets/models/intent_classifier/` — replacing the wrong-domain banking77 model that originally shipped as a PoC.
- `src/bin/train-t2.rs`
  - train-t2: trains the T2 "molecular" coordinator on T1 output features. Loads `starter.nanobyte`, runs each kova_intents train.csv example through all 4 T1 models to build 8-float feature vectors (`T2_FEATURE_DIM`), assigns routing labels — code_responder (0) for the six action-oriented intents (fix_bug, add_feature, write_tests, review_code, run_command, search_code) vs prose_responder (1) — trains a linear classifier via SGD (300 epochs), and saves weights.bin + bias.bin + config.json to `assets/models/t2_coordinator/`.
- `src/bridge.rs`
  - bridge (f403=bridge_run): PTY proxy for the Claude Code CLI that logs sessions to `tele/` for retraining. Spawns the CLI in a pty (`portable_pty`) sized to the real terminal via `TIOCGWINSZ`, puts stdin in raw mode (a `RawMode` guard using `cfmakeraw` that restores termios on drop), pumps stdin→pty and pty→stdout bidirectionally, and strips ANSI/VT escapes (CSI, OSC, bare ESC+char) from captured output so the stored training text is clean.
- `src/browser.rs`
  - Kova screen automation — vision-based dual-browser Gemini prompting (feature-gated on `browser`, screen capture macOS-only). Drives two Firefox Gemini windows in an interleaved pipeline: type a prompt into one, switch while it generates, then hover the generated image → click the download button → start a new chat. Pure pixel heuristics, no DOM access: `load_prompts` parses `### label — ...` + "Create a ..." lines from a markdown file; `capture_screen` uses CoreGraphics and converts BGRA→RGBA; `screen_hash` is a cheap stride-997 weighted sum for change detection; `find_generated_image` scans 64px blocks in the middle ~60% of the screen for maximal color variance; `find_download_button` diffs before/after-hover frames near the hover point. Input goes through enigo mouse/keyboard with AppleScript window focus.
- `src/c2.rs`
  - kova c2: T212-tokenized hive orchestration (f18–f23). `T212` is a clap `ValueEnum` over the six intent tokens (f18 build/release, f19 test, f20 full-pipeline, f21 tunnel-update, f22 setup-roguerepo, f23 cloudflare-purge) with `f347` mapping each to its intent constructor and `f349` flagging the three that run locally. `f350` lists the hive nodes (lf, gd, bt, st, mm); `f351`/`f352` do Wake-on-LAN by hardcoded MAC (st/n3 has no WoL); `f353` resolves the project path (arg → `$KOVA_PROJECT` → cwd) and `to_worker_path[_local]` rewrites `~/hive-vault` paths onto the worker's shared/local bases. `f354` (kova_c2_run) builds a plan via `plan::t3::f14` and either runs it locally through `compute::t6` or, for broadcast, requires the project to live under `~/hive-vault` and delegates to `f356` — the one-command sync + build: tar-stream for full sync when a dir is missing, rsync for incremental, then parallel broadcast to reachable non-core nodes from `inspect::f359`.
- `src/cargo/mod.rs`
  - Unified cargo executor — one copy of check/clippy/test shared by pipeline, factory, moe, and academy. `f306` runs cargo with args in a directory returning `(success, stderr)`; `cargo_check` / `cargo_clippy` (`-D warnings`) / `cargo_test` (merges stdout into the output) wrap it. `f307` extracts the core error line (`error[E…]` or first `error` line) as a key for fix-loop detection, `f308` truncates, `f309` extracts the first ```rust (or bare ```) block, `f310` heuristically detects binary-vs-library prompts ("cli", "fn main", "prints"...), and `f311` builds the codegen system prompt (std-only, single rust block, no filler, string-type-matching warnings). Tests cover the extraction/truncation/prompt helpers plus a real cargo check/clippy/test run on a generated temp lib.
- `src/cargo/sandbox.rs`
  - Temp project scaffolding for sandbox compilation (used by pipeline, factory, moe, academy). `f312` writes a minimal `Cargo.toml` (package "gen", edition 2021) plus `src/main.rs` or `src/lib.rs` — lib code gets a `#![allow(dead_code)]` prefix; `f313` is the validation variant that infers binary-vs-lib from the rel_path, and `f314` is a simple always-lib wrapper around `f312`. Includes tests asserting the written layout and prefix behavior.
- `src/cargo_cmd.rs`
  - Tokenized cargo commands with compressed output for AI context (token in → cargo execute → compress out). `t99`=CargoCmd is a clap `ValueEnum` mapping x0–x9 to build/check/test/clippy/run/build-release/clean/doc/fmt-check/bench; `t100`=CargoResult carries r0–r7 (cmd token, project token, ok, duration, warning count, error count, compressed output, "pass/fail/ignore" test counts). `PROJECT_MAP` aliases p0–p9 to workspace crates (p7 "exopack" retired after absorption into kova); `f353`/`to_project_token` resolve both directions. `f133` (cargo_exec) loads a build preset from config, assembles args (clippy's `-- -D warnings` split to after package/features), runs with `--message-format=json` where supported, and compresses the output: `compress_json_messages` parses compiler-message JSON into `E[E…]`/`W[E…]` file:line lines (primary span only, paths stripped by `compress_path`, capped at 15 with `...+N`), `parse_test_line` regexes the test runner's unstructured lines for the pass/fail/ignored summary, `FAIL:` names, and panics, and `compress_output_text` is the text-mode fallback for commands without JSON support. `f134` (cargo_exec_multi) runs one command across many projects on parallel threads via an mpsc channel, `f135` (cargo_exec_chain) runs a command list sequentially with stop-on-first-error. `f136` (cargo_cmd_dispatch) is the public dispatcher handling chain mode, `--all` mode (parallel across every PROJECT_MAP crate), and single-project runs — with remote-only crates (per `config::is_remote_only`) routed to `f361` (remote_build), which SSHes into the configured build node, runs cargo in the hive shared workspace, and re-compresses the JSON output locally. Tests cover the JSON compression variants (error/warning codes, artifact/notes skipping, test results, caps, path stripping), the text fallback, alias round-trips, and token parsing.
- `src/ci.rs`
  - CI mode — headless quality gate: watch for changes, run check/clippy/test. `t114` config (project_dir, watch_interval_secs, run_clippy, run_tests; defaults 5 s interval, clippy+tests on) and `t115` structured result (passed, check_ok, clippy_ok, tests_ok, errors, duration_ms). `f177` (ci_check) runs the three stages in order — cargo check, then clippy `-D warnings`, then tests — skipping later stages when check fails and collecting truncated stderr (10-line cap with `... (N more lines)`) into errors. `f178` (ci_watch) runs once at start, then polls: it snapshots mtimes of every `.rs` under `src/` (recursive `walk_rs_files`) plus `Cargo.toml` and re-runs `f177` when anything changed. `f179` (ci_once) is a single pass with default config; `f180` (print_ci_result) prints `ci: PASS/FAIL (Nms)` plus per-stage ok/FAILED lines and collected errors. Tests scaffold real temp cargo projects (valid, broken, nested src dirs, clippy-only) and assert the stage outcomes.
- `src/cluster.rs`
  - Backward-compat shim: a one-line re-export of `crate::inference::cluster::*` so existing `crate::cluster::` paths (the `T193` hive, `T191` task types, dispatch API) keep working after the cluster code moved to `inference/cluster.rs`.
- `src/codegen/fix_loop.rs`
  - Shared fix-loop logic — one copy used by pipeline and strategies. `fix_and_retry_local` builds a fix prompt from `pipeline::f296` (stage + categorized error block), appends the project's Cursor rules (`cursor_prompts::f111`) to a "You fix Rust {stage} errors." system prompt when present, and delegates to local Kalosm inference (`inference::f80`). `f331` is the cluster-dispatch version for factory/moe/academy: it builds the fix prompt with the compiler error + current code, and when `stuck` (the same error recurred) escalates to `cluster.speculative_dispatch` with the full previous-error history (each attempt truncated to 100 chars via `cargo::f308`) and an explicit "use a DIFFERENT approach" instruction, otherwise a plain `cluster.dispatch` — both on `T191::FixCompile` — extracting the fixed code out of the ```rust block via `cargo::f309`.
- `src/codegen/helpers.rs`
  - Shared codegen helpers — one copy, no duplication across factory/moe/academy/pipeline. Four thin delegations into `crate::cargo`: `f327` (extract_rust_block, first ```rust or bare ``` block via `cargo::f309`), `f311` (codegen system-prompt builder), `f310` (binary-vs-library prompt detection), and `f330` (truncate via `cargo::f308`).
- `src/codegen/mod.rs`
  - Codegen module root — shared infrastructure for all strategies. Declares `helpers` (extract_rust_block, f311, truncate), `fix_loop` (feature-gated on `inference`), and `strategies` (the strategy layer), re-exporting the helper trio at top level as `extract_rust_block`/`f311`/`truncate`.
- `src/codegen/strategies/mod.rs`
  - Strategy layer for composable codegen orchestration. Currently holds only `T211` — the shared config for all strategies (max_fix_retries, run_clippy, run_tests, run_review, num_ctx) whose `Default` pulls the DDI fix-loop cap and clippy toggle from `config::f213`/`f214` and fixes context at 8192 tokens. The module doc frames the plan: T181, MoE, Academy, and Gauntlet become `CodeGenStrategy` impls over a shared `CodeGenInfra`, but the trait itself is not defined here yet — `strategies/` is otherwise empty.
- `src/codegen_moe/assembler.rs`
  - Layer 1 of the MoE codegen pyramid — assemblers that understand how multiple experts' outputs fit together. `AssemblerKind` (Web, Cli, Infra, Inference) with `Assembler::for_subtask` mapping subtask kinds to a domain: Web→Web, Cli→Cli, Inference→Inference, and everything else (Scaffold, Types, Errors, Storage, Tests)→Infra. `system_prompt()` carries the per-domain assembly instructions — axum 0.7 + tower-http + `Arc<State>` for web, clap 4 derive + anyhow dispatch for CLI, Infra covering Cargo.toml (edition 2024, MSRV 1.85, opt-level z/lto/strip/panic-abort release profile), CLAUDE.md, the prod+test single-binary pattern, thiserror error enums, and sled+bincode+zstd storage, Inference covering candle GGUF loading + NanoSign BLAKE3 verification + MoE tier routing.
- `src/codegen_moe/compiler_teacher.rs`
  - Stage-3 flywheel: captures (bad, error, good) training pairs from Sponge Mesh corrections — every mesh retry that succeeds becomes one pair. Storage is a `LazyLock` redb singleton at `~/.kova/training/compiler_pairs.redb`, keyed by the blake3 hash of the error string with the value a bincode+zstd(level 3) serialized `CompilerPair` (bad_code, error, good_code, expert, timestamp). `save_pair` inserts after a successful retry; `lookup_hint` scans the table for the most recent pair for a given expert (experts inject it into prompts so they don't repeat known pitfalls); `all_pairs` feeds `kova train-data`; `dump_training_data` prints total/per-expert stats, the top-10 most frequent first-line errors, and the full JSONL.
- `src/codegen_moe/expert.rs`
  - Layer 0 — DeepSeek-MoE-style shared + routed experts ("more smaller experts > fewer larger experts"). `ExpertKind` lists 21 specializations: `RustBoilerplate` (shared, always active) plus routed experts for Cargo.toml, CLAUDE.md, struct/enum/thiserror definitions, axum router/handlers, clap parser/subcommands, sled read/write, bincode, zstd, reqwest, candle model load/forward, NanoSign verification, and unit/integration/exopack-gate tests. `SharedExpert::generate` always runs first producing only boilerplate (Unlicense header, imports, module docs). `Expert::for_subtask` activates 1–4 fine-grained experts per subtask kind, and `generate_with_context` composes the prompt from subtask context + the shared boilerplate + optional Sponge Mesh error context ("Previous attempt failed: … Do not repeat the same mistake") + a flywheel hint from `compiler_teacher::lookup_hint`. Each kind has a hand-written system prompt (P13 compressed naming, NanoSign's 36-byte NSIG+BLAKE3 trailer check, bincode 2.0 APIs, etc.) plus a description and default output path (src/types.rs, src/error.rs, src/storage.rs, src/inference.rs, src/tests.rs, …). Inference goes through `providers::f199` (the `T129` provider dispatch) with a local ```rust block extractor.
- `src/codegen_moe/extract.rs`
  - Training-data extraction from the 15-repo portfolio so each expert can be trained on its own codebase slice. `extract_all` walks a hardcoded repo list (kova, cochranblock, approuter, oakilydokily, rogue-repo, ronin-sites, pixel-forge, exopack, any-gpu, whyyoulying, ghost-fabric, call-shield, pocket-server, wowasticker, provenance-docs), emits each Cargo.toml as a CargoToml pair, and walks `src/` (skipping target/vendor/.git) via a recursive `walkdir`. `categorize_content` assigns file content to experts: brace-matched struct blocks → StructDef, enum blocks → EnumDef (or ThiserrorEnum when `#[error` is present), `#[test]` + the following fn → TestUnit (via brace-matching `extract_block`/`extract_next_fn`), axum files' `pub async fn … State(` handlers → AxumHandler, sled files with `.get(`/`.scan_prefix` vs `.insert(`/`.remove(` → SledRead/SledWrite, and clap Parser files → ClapParser. Prints per-expert pair counts.
- `src/codegen_moe/mesh.rs`
  - Sponge Mesh correction layer — runtime inference correction, explicitly NOT training-time load balancing (no Switch Transformer / Expert Choice). `MeshResult` = `Success(LayerOutput)` | `Exhausted { retries, last_error }`. `SpongeMesh::new(max_retries, base_backoff)`; `jittered_backoff` adds ±25% jitter using SystemTime nanos as cheap entropy (no rand crate) so parallel experts don't retry in lockstep. `validate` fails empty code, error-comment-only output, and `compile_ok == Some(false)`. `dispatch` re-runs the closure with jittered backoff on failure (a TODO notes plain dispatch doesn't yet feed error context back). `dispatch_with_context` is the full mesh: it passes the previous attempt's error into the next attempt's prompt, and on any successful retry saves the (bad, error, good) triple to the Compiler Teacher flywheel via `save_pair`, mapping the expert name back to an `ExpertKind` (defaulting unknowns to RustBoilerplate).
- `src/codegen_moe/mod.rs`
  - Module root for hierarchical MoE codegen — renamed from `pyramid` on 2026-05-06 ("pyramid" now names only the subatomic T1/T2/T3 architecture in `src/swarm/` + `src/nanobyte.rs`; long-term the experts' Claude-API calls are meant to converge into T3 cellular models per KOVA_BLUEPRINT Phase 4). Architecture: Layer 2 Router decomposes the task, Layer 1 Assemblers combine outputs, Layer 0 always-active SharedExpert + routed Experts, Sponge Mesh correction, Compiler Teacher flywheel (research basis: DeepSeek-MoE, THOR-MoE, Expert Choice, Sponge Mesh). Shared types: `Task` (description, project_name, `TaskTarget` Cli/Web/Lib/Full), `Subtask` (`SubtaskKind` Scaffold/Types/Storage/Web/Cli/Errors/Tests/Inference + context + parent_task), `LayerOutput` (code, file_path, source_expert, generation_ms, compile_ok, error_output, retry_count), and `PyramidResult`. `run()` orchestrates the whole pipeline: `Router::decompose` → per subtask, `SharedExpert::generate` plus parallel Expert threads over mpsc, each under its own `SpongeMesh` (3 retries, 2 s base backoff) via `dispatch_with_context`, with the shared boilerplate prepended to every successful expert output → validation gate (`validate_outputs` writes all outputs into a temp crate, merging same-file outputs, generating a Cargo.toml if absent, and running `cargo check`) → `PyramidResult` with totals. `write_outputs` groups by file_path, merges same-file code, and runs `cargo fmt`. Tests cover router decomposition, expert selection, and mesh validation/retry/context behavior.
- `src/codegen_moe/router.rs`
  - Layer 2 — the coarse-grained routing decision ("what kind of project is this?"). `Router::decompose` is a static, non-LLM decomposer: every task always gets Scaffold + Types + Errors subtasks; the target adds Cli (clap derive) and/or Web (axum) — `Full` adds both; keyword sniffing on the lowercased description ("data/store/cache/persist/database/save") appends a Storage subtask (sled + bincode + zstd) and ("ai/model/inference/predict/classify/llm") appends Inference (candle GGUF + NanoSign); Tests is always last. Each subtask's context string embeds the project name and task description for the assembler/expert prompts.
- `src/compute.rs`
  - Compute layer — plan executor (f15). `t6`=Executor (unit struct) runs a `t3` plan step-by-step, returning `Vec<t7>` (ActionResult: s10=name, s11=ok, s13=error). `f16` maps each `t5` action to a shell invocation: `CargoCheck`/`CargoBuild { release }` go through `cargo_cwd_args` — when the plan resolves a build preset (explicit `s7` name or inferred from the directory name via `config::infer_preset_name`), cargo runs at the workspace root as `-p <package> --target … --features …`, with `CARGO_TARGET_DIR` pinned inside the project for presets flagged `target_dir_in_project`; otherwise a plain run in the project dir. `CargoTest` is a bare `cargo test` (main binary: no exopack). `ApprouterUpdateTunnel`/`ApprouterSetupRoguerepo` run the `approuter` binary with `--update-tunnel`/`--setup-roguerepo` in the plan's `approuter_dir` (failing cleanly when unset), and `Custom { cmd, args }` runs an arbitrary command in the project dir.
- `src/config.rs`
  - Config center — `~/.kova/config.toml`, path helpers, bootstrap, and build presets. `load_config` parses config.toml into a `ConfigFile` with lenient per-section defaults (paths, serve, models, orchestration, build, cursor, inference, hive, offload, cluster). Path accessors: f97 home, f98 kova_dir (`~/.kova`), f99 prompts_dir, f100 db_path (redb; a deprecated `sled_path` alias remains), f101 models_dir, f103 backlog_path (`KOVA_BACKLOG_PATH` or `~/.kova/backlog.json`), f94 default_project (`KOVA_PROJECT` > config > cwd), f95 discover_projects (parses `[workspace]` members from the root Cargo.toml plus a fast `ls -1` scan for sibling Cargo.toml dirs, sorted by name), f104 workspace_root (env > config > walk-up from the project to a `[workspace]` manifest). Models: `T89`=ModelRole (Router/Coder/Fix), f78 model_path_for_role (env `KOVA_MODEL_*` > config `[models]` > `custom-model.gguf` under models_dir), f102 inference_model_path (`KOVA_INFERENCE_MODEL` > f78(Coder)), f216 ollama_url (`OLLAMA_HOST`), f217 default_model (`OLLAMA_MODEL`). Orchestration/inference flags: f207 router_resident (true), f208 specialist_idle_unload_secs (300), f213 max_fix_retries (2 — DDI cap), f214 run_clippy (true), f209 model_cache_size (env or 2), f212 model_idle_unload_secs, f211 router_structured (true), f210 code_gen_structured (experimental), f215 cursor_prompts_enabled, f220 fast_localhost (skip TLS on loopback). Build: `T88`=BuildPreset (package, target, features, target_dir_in_project), f105/f106 preset load/all, f107 infer_preset_name (directory name), `is_remote_only` + `remote_build_node` (default "gd"). Serve: f108 bind_addr (`KOVA_BIND` > config > 127.0.0.1:3002). Hive/offload: f218 hive_local_base (`/tmp/hive-build`), f219 hive_shared_base (`/mnt/hive`), offload helpers (90% threshold, target node `bt`, archive `/mnt/hive/archive`), `cluster_nodes` (`ClusterNodeConfig`: id/host/port 3002/model/tier "mid"/role "batch"). f109 bootstrap creates `~/.kova`, `prompts/`, `models/`, a default config.toml (commented sections plus presets for oakilydokily, mural-wasm, kova, approuter, cochranblock, rogue-repo), a default system.md with the P12 style rules, persona.md, and an empty backlog.json. f110 load_prompt reads `~/.kova/prompts/{name}.md`. Every fN is re-exported under a human-readable alias (`default_project`, `workspace_root`, `orchestration_max_fix_retries`, …).
- `src/context.rs`
  - Project context for code gen (f82=load_project_context). `T90` holds Cargo.toml + the contents of mentioned `.rs` files + optional recent changes (`recent_changes::t86`). `f82`/`f82_with_recent` read Cargo.toml, extract `.rs` filenames mentioned in the user input (`extract_mentioned_rs_files` — a hand-rolled scanner that backtracks up to 50 chars before each `.rs`, requires an alphanumeric/underscore stem, and dedupes), read each file (preferring `src/` over the project root in `try_read_rs_file`), and attach recent changes via `recent_changes::f86` when `within_minutes` is set. `f83` (target_file_hint) returns the first mentioned file for the Apply target. `f112` (format_context) renders everything as a `--- Project context: ---` block with ```toml/```rust fences, appending the tokenized recent-changes list from `recent_changes::f87` to keep the LLM on task. Tests cover extraction (dedupe, underscores), src-over-root preference, and formatting.
- `src/context_loader.rs`
  - Conversation persistence for chat. `t91` Message (role + content) is stored under a single key `conv:default:messages` in the storage store, capped at 20 messages. `f73` (store_message) appends and drops the oldest beyond the cap; `f74` (load_messages) reads the conversation back, defaulting to empty.
- `src/context_mgr.rs`
  - Token-aware context window manager. `t111` ContextBudget (max_tokens, system_reserved, tool_reserved, saturating `remaining()`). `f170` (estimate_tokens) is a chars/4 round-up estimate — deliberately no tokenizer crate for local GGUF models. `f171` (trim_conversation) trims statically: it splits the conversation at `User:/Assistant:/Tool results:` boundaries (`split_turns`), always keeps the first (original question) and last turns, fills the remaining budget backwards from the second-to-last, and inserts `f173`'s (summarize_old_turns, a turn-count-based summary) `[earlier context trimmed: …]` line for the dropped middle; it falls back to front-truncation when there are ≤2 turns. `f172` (trim_tool_output) keeps 60% head / 30% tail with a `[truncated: ~N chars omitted]` marker (clamped so head+tail never overlap short output; tiny budgets yield just `"[truncated]"`). `f380` (compact_context) is the LLM-powered tier: when the conversation exceeds 80% of the available budget (COMPACT_THRESHOLD), it keeps the first + last 4 turns (COMPACT_KEEP_RECENT) intact and summarizes the old turns through `inference::f382` streaming (static f173 fallback when the `inference` feature is off or inference returns empty), reassembles with a "[Context compacted — N turns summarized into brief/digest]" header, and still falls back to the static f171 trim if the result is over budget. Tests cover token estimation (incl. multibyte UTF-8), budget math, trim/summarize behavior, and the compaction threshold/keep-recent constants.
- `src/cursor_prompts.rs`
  - Loads Cursor prompts for injection into model context. `baked_prompts()` inlines five rule files from `assets/prompts/` (blocking, augment-not-intent, tokenization, hosting-schematic, header-writer) plus `docs/src/compression_map.md` via `include_str!`, so the core rules ship without any external files. `f111` (load_cursor_prompts) returns empty when `config::cursor_prompts_enabled()` (f215) is off; otherwise it concatenates, in order, the baked rules, `~/.cursor/rules/*.mdc`, `~/.cursor/shared-rules/*.mdc`, the workspace's `.cursor/rules/*.mdc`, a fixed list from `~/.cursor/protocols` (PROTOCOLS.mdc, PROTOCOL_COMPLIANCE.md, UNIFIED_PROTOCOLS.md, README.md, env_token_map.md), the `.cursor/rules` + `.cursor/protocols` of every project from `config::discover_projects()`, each skill's `SKILL.md` under `~/.cursor/skills-cursor` and `~/.cursor/skills`, the workspace `AGENTS.md`, and `protocol_map.md`/`compression_map.md` (first found under `docs/` or the workspace root) — each as a `--- <path> ---` section. Tests prove the baked content is non-empty and contains the P20 blocking rules and the compression map (f81/run_code_gen_pipeline, f78/model_path_for_role) and that the serve system-prompt format propagates them.
- `src/daemon.rs`
  - `kova node` — swarm worker daemon, Phase 1 stub. Declares a `protocol` module via `capnp::generated_code!` over the Cap'n Proto schema `schema/kova_protocol.capnp` (zero-copy binary protocol for c2-core ↔ kova-node: a `Command` struct with `commandType`, `workingDir` defaulting to `/mnt/hive/projects`, and a payload union of `cargoBuild`/`fileSync`/reserved `unset`). `run()` only prints "schema loaded, daemon stub (Phase 1)" — the network listener is explicitly deferred.
- `src/elicitor.rs`
  - Elicitor module for the clarification flow used by GUI and serve. `T177` is the parsed user reply: `Choice(index)` (a→0, b→1…), `Freeform(text)`, `Confirm(bool)`, `Cancel`. `f302` formats a question with optional choices as "(a) X (b) Y (c) Z" (letters a–e, `?` beyond five). `f303` parses replies: empty/cancel/stop/abort → Cancel; y/yes → Confirm(true); n/no → Confirm(false); single letters a–e or 1–5 (bounded by `num_choices`) → Choice; other numbers ≥1 → Choice(n−1); otherwise Freeform. `f304` builds the restatement "I'll {action} in {target}. Proceed? (y/n)". Tests cover the formatting and every parse branch.
- `src/error.rs`
  - T176 — the unified error type meant to replace `Result<T, String>` and raw anyhow across the codebase. A thiserror enum with `Storage` (`#[from]` storage::E0) and `Io` (`#[from]` std::io::Error) plus string-payload variants Inference/Cluster/Cargo/CodeGen/Config/Provider/Tool/Serde/Other, each displayed with a "kind: {0}" prefix (Storage/Other bare). `From` impls bridge `String` → Other, `serde_json::Error` → Serde, and `anyhow::Error` → Other; `T176Result<T>` is the convenience alias. Tests cover the conversions and Display prefixes.
- `src/exopack/agent_loop_tests.rs`
  - Diamond-pattern end-to-end suite for the agent loop. `f423` (run_agent_loop_suite) runs 7 scenarios by spawning the real `kova chat` binary with `KOVA_INFERENCE=mock` + `KOVA_INFERENCE_MOCK_FILE` pointed at a per-scenario JSONL of canned responses, asserting on filesystem side effects rather than REPL stdout chrome: plain text terminates with no tool; write_file then Done (marker file lands on disk); read_file then Done (dispatch visible in stderr); write→read chaining; a grep call; recovery from a malformed tool JSON (graceful exit, no dispatch); and overconsumption — one canned response but a tool forces a second inference call that walks past EOF into the mock's "[mock: end of script]" guard, ending the loop. The `run_chat` harness clears the environment (so KOVA_PERMS/KOVA_MODEL/KOVA_TOOL_ROUTER_PATH can't leak), pipes the prompt and EOFs stdin, drains stdout/stderr on dedicated threads so pipe buffers can't deadlock the child, and polls `try_wait` under a 30 s cap (`KOVA_TEST_AGENT_TIMEOUT_SECS` override) killing the child on timeout. `tool_call_response` builds the f140 parser format ("Calling tool.\n```json …```") through serde_json so embedded quotes/newlines stay valid JSON, and the suite resets the in-process mock cursor at entry as belt-and-braces against cross-suite cursor leaks.
- `src/exopack/ats_fixtures.rs`
  - Exopack capability: ATS fixture generator. Renders self-contained HTML strings mirroring the DOM conventions of five applicant-tracking systems — Greenhouse (label-for + bracketed `job_application[first_name]` keys, the literal-trailing-space `urls[Github ]` quirk), Lever (aria-label-only fields, combined name, no last/address sub-fields), Workday (sibling `wd-label` spans, aria-label semantics, dynamic `input-<num>-<uuid>` ids, late hydration, rebuild-on-focus), iCIMS (legacy fieldset/legend), Ashby (React shapes, `role="combobox"` masquerading as select) — for CDP-driven autofill test benches (loadable via chromiumoxide `Page::set_content`, no external CSS/JS/network). `FixtureOpts` dials in the adversarial patterns: `late_hydration_ms` (setTimeout-deferred hydration), `dynamic_ids` (deterministic djb2-hash ids so snapshots stay reproducible), `rebuild_on_focus` (a `focusin` listener that destroys and recreates the focused field's parent). All fixtures share the canonical 9-field profile inventory (12 slots: first/last, email, phone, linkedin, github, street1/city/postal_code, work_authorization, freetext, salary_expectation — the last was an out-of-vocab decoy until atsisbroken's Phase G Tier 3 classifier route landed). `render(vendor, opts)` produces the page; `expected_keys(vendor, opts)` returns the (id, classifier_key) pairs to assert against, with per-vendor deviations (Lever/Ashby combined `full_name`, Ashby's single-line `address` vs structured `street1`). Tests assert per-vendor rendering, label uniqueness, dynamic-id determinism, hydration/rebuild emission, key coverage, absence of external resources, and JSON escaping.
- `src/exopack/baked_demo.rs`
  - Baked-in demo (f95=run_baked_demo): replicates every intended use of Kova with zero user input — no recording, programmatic iteration of dev cycles. Steps 1–5 drive the CLI (`bootstrap`, `prompts`, `model list`, `recent --minutes 60`, `c2 nodes`) via `run_kova` on `tokio::spawn_blocking` with HOME/KOVA_PROJECT/KOVA_PROJECTS_ROOT isolated, failing on nonzero exit with stderr attached. Step 6 spawns `kova serve` as a child (isolated HOME, `KOVA_BIND` on the given port, `KOVA_DEMO_DIR` under `~/.kova/demos`, output nulled) and sleeps 2 s to settle. Steps 7–14 are GETs through the `interface::f81` client: `/` (web client), `/api/status` with a hard 2 s latency baseline check, `/api/project`, `/api/projects`, `/api/prompts`, `/api/backlog`, `/context/recent`, `/build/presets` (each via `get_ok` with a 5 s timeout). Steps 15–18 POST the tokenized FullPipeline intent body (`s0`/`s1`/`s2` constraint tags), a backlog entry, a best-effort `/api/diff` (200 not required — no project), and a 3-action `/api/demo/record`. Kills the child, then verifies the `baked-demo.json` artifact landed in the demo dir. Returns Ok(()) only if every step passes.
- `src/exopack/cc_features.rs`
  - Claude Code feature replication via MCP stdio (Diamond Rust Binary Architecture): kova-test (this side) spawns `kova mcp --project <project>` with HOME isolated to a tempdir and drives it over JSON-RPC 2.0 on stdio — the exact external interface Claude Desktop uses, no lib internals. `T216`=McpHarness owns the child + stdin + a stdout reader thread that pumps lines into an mpsc channel (so f404 can `recv_timeout`) + a stderr drain thread into an `Arc<Mutex<String>>` so the 64 KB pipe buffer can never deadlock the child; `Drop` kills + reaps the child (drain threads are deliberately not joined — they exit on EOF, and joining could block). `f403`=mcp_spawn performs the spec handshake (`initialize` with protocolVersion 2024-11-05, then the `notifications/initialized` notification with no reply read). `f404`=mcp_request sends id-tagged requests with a hard 10 s `MCP_REQUEST_TIMEOUT`, skips notifications (absent/null `id`) and out-of-order ids, and decorates every error with the child's accumulated stderr via `err_with_stderr`. `f405`=mcp_call_tool wraps `tools/call`, extracting the first text content and turning `isError`/RPC errors into Err. `send_raw` feeds malformed lines for protocol-level negative tests; `expect_fail`/`expect_fail_with` negate tool calls (the latter also asserts an error substring). `f406`=run_cc_suite walks the `SCENARIOS` registry — 82 (label, fn) pairs, each scenario getting a fresh TempDir project + fresh HOME + fresh subprocess, per-scenario timing, `(all_passed, report)` out: MCP primitives (handshake, tools/list with ≥15 named tools, unknown-method error, jsonrpc 1.0 rejection); read_file 8 (content, line numbers, offset, limit, offset+limit, missing path, empty file, no path); write_file 5 (new, parent-dir creation, overwrite, missing content/path); edit_file 5 (unique replace, non-unique match fails, missing match fails, surrounding content preserved, no args); glob 4 (basic, extension filter, recursive `**`, clean no-match); grep 4 (match, file path included, no matches, multi-file aggregation); exec 5 (stdout, nonzero exit, `pwd` = project dir, stderr capture, no command); memory_write 2 (append + persist to `~/.kova/memory.md`); undo_edit 2 (restore after edit, no-checkpoint fails); code_outline 2 (fns + structs); 4 multi-step flows (write→read, write→edit→undo, grep→read, write→glob); todo_write 5 (single, multi with "N todos saved", invalid status, empty content, full-list replace); agent 5 (queue with description/prompt, general-purpose default, subagent_type, run_in_background, required args); ask_user_question 4 (question, options rendering, invalid options JSON, option without label); web_fetch 5 and web_search 4, backed by `spawn_echo_server`/`spawn_fixed_server` — one-shot `TcpListener` HTTP servers on 127.0.0.1:0 that echo the request line (proving GET + percent-encoding of `hello world & friends` → `hello%20world%26`) or return a fixed 8 KB body for the `max_bytes` truncation test; plan mode 15 (enter, exit echoes plan, exit requires plan arg, write/edit/exec/legacy-bash/undo all blocked with "blocked: in plan mode" + on-disk verification that nothing leaked, read/grep/glob/todo/memory still allowed, exit re-enables write, enter idempotent); error handling 3 (unknown tool, missing required arg, nonexistent path).
- `src/exopack/checkpoint.rs`
  - Test harness for the checkpoint/undo contract — mirrors kova f383/f384 in pure std with no sled dependency, testing file-level behavior. `t73` is a checkpoint store backed by `HashMap<PathBuf, Vec<(timestamp_ms, content bytes)>>`. `f121` snapshots a file before a write/edit (None for nonexistent files — new files have nothing to restore), `f122` restores the latest checkpoint (undo), `f123` restores the Nth checkpoint (0 = oldest, negative from the end, range-checked), `f124` counts checkpoints, and `f125` verifies the file matches its latest checkpoint, reporting byte-level drift. All operations return `t74` (ok, detail message, bytes affected). 8 tests cover the full cycle (snapshot → modify → undo restores v1), new-file no-op, multiple checkpoints undoing to the latest, undo by index (0/1/-1), out-of-range index, drift detection after an un-checkpointed modification, undo with no checkpoint, and binary files (0–255 byte range round-trip).
- `src/exopack/compaction.rs`
  - Test harness for context-compaction patterns — pure std, no inference dependency; tests the structural contract that kova's `context_mgr::f380` implements. `t75` is the budget (s91 total tokens, s92 reserved for system/tools), `t76` the result (triggered, before/after token counts, before/after turn count, output text). Constants `COMPACT_THRESHOLD` (0.80) and `COMPACT_KEEP_RECENT` (4) match kova's. `f130` estimates tokens as chars/4 (matching kova f170's deliberate no-tokenizer heuristic), `f131` splits a conversation into turns on `User:`/`Assistant:`/`System:` line prefixes, and `f132` runs compaction with a pluggable `FnOnce(&str) -> String` summarizer so tests inject deterministic summaries without inference: under threshold or with ≤5 turns it's a no-op; otherwise it keeps the last 4 turns verbatim, summarizes the rest, and emits a `[Context compacted — N turns summarized]` header + `Summary of earlier conversation:` block (the summary counts as 1 turn). 8 tests cover no-compaction under threshold, over-threshold shrinkage with the marker strings, recent turns preserved verbatim, too-few-turns guard, token estimation, turn splitting, report metrics (12 turns → 4+1), and pluggable summarizer content.
- `src/exopack/demo.rs`
  - Demo mode: action-script recording for UI testing. `t66`=DemoAction is a serde internally-tagged ("kind", snake_case) enum of one recorded action — `WebClick` {selector, ts_ms}, `WebInput` {id, value}, `ApiCall` {method, path, optional body_summary} (web or egui), `EguiSend` {text}, `EguiConfirm` {intent}, `Screenshot` {path relative to demo dir}. `t67`=DemoRecord (name, source "web"|"egui", actions, started_at) is written to `~/.kova/demos/{name}.json`: `new()` stamps epoch seconds via `chrono_now` (SystemTime, no chrono crate), `push` appends, `save` sanitizes the name (`/`, `\`, `:` → `_`) and writes pretty JSON, `load` reads it back. `f90`=demo_dir resolves `KOVA_DEMO_DIR` or `~/.kova/demos`. 3 tests cover the round-trip, tagged serialization (`"kind":"egui_send"`), and filename sanitization.
- `src/exopack/devtools.rs`
  - Headless browser console checks + screenshots via chromiumoxide CDP. `browser_config()` tries chromiumoxide auto-detection first; on "Could not auto detect" it downloads Chromium via `BrowserFetcher` into the OS cache dir's `chromiumoxide/` subdirectory and rebuilds the config around the fetched executable (header notes Linux needs libnspr4/libnss3 for the fetched binary). `f74`=check_console_errors launches the browser, drains the CDP event-handler stream on a spawned task, and per URL runs `check_one_url`: two event listeners — `Runtime.consoleAPICalled` (error/warning types, arg values joined) and `Log.entryAdded` (after `Log.enable`, error/warning levels) — pump messages into an unbounded mpsc channel while the page navigates and settles 2 s, then the channel is drained into URL-prefixed error strings. `f75`=capture_screenshots navigates each (name, path) pair, waits for render (8 s for the `mural` page so WASM canvas finishes, 3 s otherwise), and saves a full-page PNG via `ScreenshotParams` (Png format, full_page) — returns true only if every capture succeeds. Both functions close the browser and abort the handler task on exit.
- `src/exopack/dual_mode.rs`
  - Test harness for dual-mode inference routing — pure std, no inference dependency; tests the routing contract in isolation. `t77` is the mode enum (Local GGUF / Remote API / Auto), `t78` the routing decision (requested mode, resolved mode, model-path-exists, API-key-present, detail string). `f133` parses the env var value case-insensitively ("local"/"remote", anything else → Auto). `f134` computes the decision: Local stays Local even when the model file is missing (detail records the missing path), Remote stays Remote even without a key, and Auto resolves to Local when the model file exists, else Remote (with or without a key — the detail notes it "will fail" when keyless). `f135` validates that a resolved decision would succeed (Local requires the on-disk model, Remote requires ANTHROPIC_API_KEY, Auto is an error since it must always resolve). 11 tests cover the parse branches, all four Local/Remote sticky combinations, all three Auto resolutions, and the invariant that Auto never stays Auto.
- `src/exopack/harvest.rs`
  - Gemini sprite harvest over raw CDP websockets — connects to an existing authenticated Chrome (`--remote-debugging-port=9222`, logged into Gemini); it does not launch Chrome by default. `HarvestConfig` (ws_url, output_dir defaulting to `for_human_review/`, class_name, sheet_count, style defaulting to "Dungeon Crawl Stone Soup"). `build_prompt` asks for a 5×6 grid of 32×32 pixel-art sprites, unique poses/weapons/armor, white background, no text. `find_gemini_ws` GETs `/json` and picks the tab whose URL contains `gemini.google.com/app` → its `webSocketDebuggerUrl`. The flow: `navigate_fresh` (Page.navigate to a fresh /app chat + 4 s settle), `type_prompt` (injected JS focusing the rich-textarea contenteditable, setting textContent, dispatching `input`; prompt is backslash/quote-escaped), `click_send` (aria-label "Send message" / data-at send / `.send-button` / mat-icon fallback, Enter keydown on the contenteditable as last resort), `wait_for_image` (polls every 3 s up to 90 s for "Creating your image" to clear and a `blob:` img with naturalWidth > 100), `extract_image` (async JS draws the blob img onto a canvas → `toDataURL('image/png')`, base64-decoded to disk). `harvest_one` chains prompt→send→wait→extract to `{class}_sheet_{i:03}.png` then navigates to a fresh chat; `harvest_batch` loops (class, count) pairs, logging per-sheet failures without aborting. `launch_debug_chrome` is the opt-in launcher: pkills Chrome, copies Cookies/Login Data/Web Data/Preferences/Secure Preferences/Local State from the real macOS profile into a temp user-data-dir, launches headless=new Chrome with the debug port, and polls `/json/version` for 10 s. CDP helpers: `cdp_send` (one-shot tokio-tungstenite connect/send/read) and `cdp_eval`/`cdp_eval_async` (Runtime.evaluate with `awaitPromise`, reading `/result/result/value`).
- `src/exopack/interface.rs`
  - Test-interface primitives. `f80`=bind_random binds `127.0.0.1:0` (OS-assigned port) and returns the listener plus the base URL string; `f81`=http_client builds a reqwest client with a cookie store and redirect-following disabled — the client baked_demo and the HTTP test scenarios use against a running `kova serve`.
- `src/exopack/mock.rs`
  - On-demand mock APIs — thin wiremock wrappers. `f82`=start_mock_server starts a `MockServer` on a random port returning (server, base_uri); `f83`=mount_get and `f84`=mount_get_json mount GET handlers returning 200 with a string or JSON body; `f85`=mount_post and `f86`=mount_post_json are the POST equivalents; `f87`=mount_status mounts any-method handlers with an arbitrary status code + body. All five take a path pattern and mount onto a borrowed `MockServer`.
- `src/exopack/mod.rs`
  - Module root for exopack — the testing-augmentation layer (screenshots, video, interfaces, API mocks, triple sims, demos, feature-replication suites). `#![forbid(unsafe_code)]` plus a blanket allow for the P13 compressed-identifier lints (non_camel_case_types, non_snake_case, dead_code, unused_imports). Each capability is its own feature-gated `pub mod` — interface, mock, video, screenshot, triple_sims, devtools, demo, baked_demo, standards_check, checkpoint, compaction, dual_mode, perm_gate, harvest, ats_fixtures, cc_features, training_mine_tests, tool_call_parser, router_spec, agent_loop_tests, router_training_tests, tele_tests, smoke — so the binary can be built with only the test surface it needs.
- `src/exopack/perm_gate.rs`
  - Test harness for the tool-permission gate — pure std, no runtime dependency; tests the permission contract. `t79` is the mode (Open: all tools run unprompted / Guarded: exec + git mutations require approval), `t80` the check result (tool name, allowed bool, reason). `GIT_MUTATIONS` is the pattern list (`git commit`, `git push`, `--force`, `--no-verify`, `git reset --hard`, `git checkout --`, `git clean -f`). `f136` parses the env value (only "guarded" is case-sensitive-match; anything else → Open). `f137` does the check: Open allows everything; Guarded blocks `exec` (and the legacy `bash` alias) — with a specific "git mutation detected: {pattern}" reason when the command matches a mutation pattern, otherwise the generic approval reason — while read/write/edit/glob/grep tools stay allowed. `f138` maps `bash` → `exec` for backward compat; `f139` returns all matched mutation patterns in a command. 11 tests cover parsing, open-allows-all (even `rm -rf /`), guarded exec/bash blocking, read-tool allowance, every git-mutation pattern, safe git (`git status` blocked as exec but not cited as a mutation), the rename, multi-mutation detection (4 patterns in one command), and no-false-positives on safe commands.
- `src/exopack/router_spec.rs`
  - Contract for the tier-1 tool_router classifier (sub-100K params). `SPEC` is 8 prompt→tool pairs (show me → read_file, create → write_file, change → edit_file, find → grep, list → glob, run → exec, undo → undo_edit, remember → memory_write). Behavior is driven by whether a trained checkpoint exists on disk: `KOVA_TOOL_ROUTER_PATH` (or the default `f427` = `~/.kova/models/tool_router`) resolves → live assertions where each prompt's top-1 from `swarm::tool_router::f425` must equal the expected tool, with confidence reported but not required to clear the 0.7 floor (informational, per KOVA_BLUEPRINT §1 — the synthetic-corpus model reports lower confidences than a full-corpus retrain); no checkpoint → every entry reports `[PEND]` and the suite passes, keeping the spec preserved in code until `kova-test` after a retrain flips it live. `f419`=run_router_spec_suite runs all entries (PASS/FAIL/ERR per line, first-failure reported at the end), returns `(all_passed, report)` with timing.
- `src/exopack/router_training_tests.rs`
  - Diamond-pattern training suite for the tier-1 tool_router: each scenario trains a fresh router on synthetic examples in a tempdir and asserts classifier behavior; once it passes, `router_spec`'s PEND entries can flip to live (it picks up the checkpoint via `KOVA_TOOL_ROUTER_PATH` or the default path). 13 scenarios: `KOVA_ROUTER_TOOLS` stable ordering (must include the 8 router_spec tools), tool↔class index round-trip, unknown tools (NotebookEdit, ScheduleWakeup, ToolSearch, empty) map to None, training writes weights.bin + bias.bin + config.json, one prediction scenario per class using the same 8 prompts as router_spec (read/write/edit/grep/glob/exec/undo/memory), top-1 train accuracy ≥ 80% floor, and config.json's `class_names` matching `KOVA_ROUTER_TOOLS` exactly in order. Fixtures: `synth_examples()` delegates to `swarm::tool_router` so the test corpus and the `kova train-router --mix-synthetic` CLI share one source of truth; `fast_config` shrinks training to feature_dim 2048 / 40 epochs / lr 0.02 (sub-2 s per scenario; the CLI's Default is 8192/30); `train_synth` trains into a TempDir via `f424`, leaking the TempDir into the scenario so model files outlive the call. `f428`=run_router_training_suite runs all with per-scenario timing, returns `(all_passed, report)`.
- `src/exopack/screenshot.rs`
  - Visual regression (Sim 4): capture, compare, diff. `f70`=out_dir is `~/.cache/screenshots/{os}/{project}`; `t60`=Theme (a placeholder type — `f78`=theme_cochranblock returns the block-diagram theme). `f79`=capture_project fetches each page and renders via `devtools::f75` (headless Chromium, feature-gated), falling back to a placeholder when the devtools capture errors or the feature is off: a plain GET must succeed, then `write_placeholder_png` writes a 100×100 gray PNG. `f71`=compare_screenshots is a pure-Rust pixel diff (image crate): Lanczos3-resizes the actual to the baseline's dimensions, counts a pixel as changed when any RGB channel differs by more than the per-channel `tolerance` (i16 math), and reports `t61` (matches if diff_pct ≤ threshold, diff_pct, total/diff pixels). `f72`=generate_diff_image writes a diff PNG with changed pixels in red (255,0,0,200) and unchanged pixels as the baseline at alpha 80. `t62`=PageResult (name, passed, diff_pct, actual/baseline paths, diff image, status), `t63`=VisualReport (pages, all_passed, baselines_created/compared, `print_summary` printing per-page OK/FAIL lines). `f77`=baseline_dir (`.../baselines`). `f73`=visual_regression is the orchestrator: captures into `current/`, first run copies the capture to a new baseline (pass, "baseline created"), later runs compare and emit `{name}_diff.png` under `diffs/` on failure, and missing captures report 100% diff as "capture failed". `f76`=update_baselines copies current captures over baselines (accept the new state) and returns the updated count. 14 tests: out_dir shape, identical/different/within-tolerance comparisons, mismatched-dimension resize, baseline dir location, update-baselines copying, diff-image generation with red changed pixels, plus 3 async end-to-end tests against a local HTTP server (first run creates baselines, second run compares identical, failure writes a diff image).
- `src/exopack/smoke.rs`
- `src/exopack/standards_check.rs`
- `src/exopack/tele_tests.rs`
- `src/exopack/tool_call_parser.rs`
- `src/exopack/training_mine_tests.rs`
- `src/exopack/triple_sims.rs`
- `src/exopack/video.rs`
- `src/factory.rs`
- `src/feedback.rs`
- `src/gauntlet.rs`
- `src/git_cmd.rs`
- `src/gpu_sched.rs`
- `src/hive/config.rs`
- `src/hive/mod.rs`
- `src/hive/sync.rs`
- `src/hive/watcher.rs`
- `src/inference/cluster.rs`
- `src/inference/local.rs`
- `src/inference/mock.rs`
- `src/inference/mod.rs`
- `src/inference/providers.rs`
- `src/inspect.rs`
- `src/intent.rs`
- `src/job_queue.rs`
- `src/kernel/commands.rs`
- `src/kernel/mod.rs`
- `src/kernel/stream.rs`
- `src/lib.rs`
- `src/main.rs`
- `src/mcp.rs`
- `src/micro/academy.rs`
- `src/micro/bench.rs`
- `src/micro/candle_train.rs`
- `src/micro/kova_model.rs`
- `src/micro/logmine.rs`
- `src/micro/mod.rs`
- `src/micro/moe_tournament.rs`
- `src/micro/pipe.rs`
- `src/micro/quantize.rs`
- `src/micro/registry.rs`
- `src/micro/router.rs`
- `src/micro/runner.rs`
- `src/micro/stats.rs`
- `src/micro/template.rs`
- `src/micro/tournament.rs`
- `src/micro/train.rs`
- `src/micro/train_harness.rs`
- `src/micro/validate.rs`
- `src/moe.rs`
- `src/nanobyte.rs`
- `src/node_cmd.rs`
- `src/output.rs`
- `src/pipeline/compilation.rs`
- `src/pipeline/error_kind.rs`
- `src/pipeline/fix_loop.rs`
- `src/pipeline/mod.rs`
- `src/plan.rs`
- `src/providers.rs`
- `src/rag.rs`
- `src/recent_changes.rs`
- `src/repl.rs`
- `src/review.rs`
- `src/router.rs`
- `src/serve.rs`
- `src/squeeze.rs`
- `src/ssh_ca.rs`
- `src/storage.rs`
- `src/surface/mod.rs`
- `src/surface/serve/mod.rs`
- `src/swarm/mod.rs`
- `src/swarm/priority.rs`
- `src/swarm/t2.rs`
- `src/swarm/tool_router.rs`
- `src/swarm/train.rs`
- `src/syntax.rs`
- `src/test_utils.rs`
- `src/theme.rs`
- `src/tokenization.rs`
- `src/tools.rs`
- `src/trace.rs`
- `src/training_data.rs`
- `src/training_mine.rs`
- `src/tui.rs`
- `tests/cc_features_smoke.rs`
- `tests/integration.rs`

## Documentation
- `docs/analysis/` - Analysis reports and technical documentation
- `legal/` - Legal documents and compliance materials

> Note: Files like BACKLOG.md, backlog.json, binary-sizes.log, and audit.toml have been removed as they are no longer needed for the restructured project.